type: regex
parameters:
    boolean_operator: AND
    url:
      boolean_operator: OR
      regexes:
        - .*category.*
        - .*post.*
    content:
      boolean_operator: OR
      regexes:
        - .*bar.*
        - .*foo.*
